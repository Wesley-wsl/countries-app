import styled from 'styled-components';

export const ErrorStyle = styled.main`
    font-size: 30px;
    font-weight: 800;
    display: flex;
    justify-content: center;
    position: absolute;
    top: 40%;
    right: 45%;
    margin-top: -30px;
`;
